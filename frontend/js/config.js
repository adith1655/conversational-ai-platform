/**
 * Frontend configuration
 * Change API_BASE to your deployed backend URL in production.
 * In development, the backend runs on localhost:8000.
 */
const CONFIG = {
  API_BASE:   "http://localhost:8000",
  PHOTO_BASE: "http://localhost:8000/gallery",
};
